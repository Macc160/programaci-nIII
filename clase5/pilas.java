public class OperaciboolenaonesPila{
    public boolean validarParentesis(String[] s){
        boolean flag = false 
        pilaSignos = new Stack <>();
        
        for (int 1 = 0; i < s.leangth; i++){
        pilaSignos.push(s[i]);
     }
    int tam = pilasSignos.size();
    // recorrer la pila
    for ( int 1 = 0; i < tam; i++ ){
        if (pilasSignos.peek().equals("("))){

            cont1 += 1,
            else{
                cont2 += 1;
                flag = (cont1 == cont2 ? true : false);
            }
        }
    }

    }
}