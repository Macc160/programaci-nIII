import java.util.Arrays;

public class EjecutarLibro {
    public static void main(String[] args) {
        
        /* 
        // Creación de objetos de tipo Libro
        Libro libro1 = new Libro("frezca uncion", "pedro", 85000);
        Libro libro2 = new Libro("El Principito", "Antoine de Saint-Exupéry", 45000);
        Libro libro3 = new Libro("Don Quijote de la Mancha", "Miguel de Cervantes", 99000);
        Libro libro4 = new Libro("La Odisea", "Homero", 70000);
        Libro libro5 = new Libro("Rayuela", "Julio Cortázar", 65000);

        // Arreglo de objetos Libro
        Libro[] libros = new Libro[5];
        libros[0] = libro1;
        libros[1] = libro2;
        libros[2] = libro3;
        libros[3] = libro4;
        libros[4] = libro5;

        // Mostrar la información de cada libro
        for (Libro l : libros) {
            System.out.println(l);
        }

        // Calcular el precio total de todos los libros
        double precioTotal = calcularPrecioTotal(libros);
        System.out.println("\nPrecio total de todos los libros: $" + precioTotal);
    }

    // Método para calcular el precio total
    public static double calcularPrecioTotal(Libro[] libros) {
        double total = 0;
        for (Libro l : libros) {
            total += l.getPrecio();
        }
        return total;
        */
        
    }
}
